export function table(time :number){
    for(let i = time; i <= time; i++){
        // console.log(i)
        for(let j = 1; j <= 10; j++){
            console.log(`${i} x ${j} = ${j*i}`);
        }
    }
}
// table(9)
